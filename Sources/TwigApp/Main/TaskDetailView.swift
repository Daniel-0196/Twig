import SwiftUI
import TwigCore

/// 详情栏占位：任务选中/编辑交互后续迭代，目前只显示提示
struct TaskDetailView: View {
    var body: some View {
        Text("任务详情")
            .foregroundStyle(.secondary)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
